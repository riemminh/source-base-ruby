import { PATH_PAGE } from '../../routes/paths';

// ----------------------------------------------------------------------

const navConfig = [
  // ----------------------------------------------------------------------
  {
    subheader: 'general',
    items: [
      { title: 'profile', path: PATH_PAGE.profile },
      { title: 'go login', path: PATH_PAGE.login },
    ],
  },
];

export default navConfig;
